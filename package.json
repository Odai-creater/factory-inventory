{
  "name": "factory-inventory-service",
  "version": "2.0.0",
  "description": "Factory parts inventory management service (AWS Lambda + API Gateway + DynamoDB)",
  "main": "lambda/index.mjs",
  "type": "module",
  "scripts": {
    "start:local": "node -e \"import('./app.js')\"",
    "deploy": "bash deploy.sh"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
